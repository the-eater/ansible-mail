user = {{ mysql.user }}
password = {{ mysql.pass }}
hosts = {{ mysql.host }}
dbname = {{ mysql.mail }}
query = SELECT destination FROM virtual_aliases a JOIN virtual_domains d ON d.id = a.domain_id WHERE CONCAT(a.local_part, '@', d.name) = '%s'
